diff --git a/DATA_README.md b/DATA_README.md
index 7e07371..9363ff6 100644
--- a/DATA_README.md
+++ b/DATA_README.md
@@ -25,7 +25,7 @@ Running the pipeline also produces intermediate CSVs (`coordinates_with_indices.
 
 ## Data split
 
-Real ovitrap records span 2009-2013 and 2023-2024. The 2009-2013 seasons are used for GMM fitting, synthetic data generation, and the four-fold cross-validation training/validation rotation. The 2023-2024 season is held out entirely - it is not used in augmentation, GMM fitting, or any training or validation fold - and serves only as the held-out test set against which every reported metric in the paper is computed.
+Real ovitrap records span 2009-2013 and 2023-2024. The 2009-2013 seasons are used for GMM fitting, synthetic data generation, and the four development-stage training/validation trials. The 2023-2024 season is held out entirely - it is not used in augmentation, GMM fitting, or any training or validation fold - and serves only as the held-out test set against which every reported metric in the paper is computed.
 
 ## Data availability
 
diff --git a/README.md b/README.md
index 7dd1fc0..1652d3c 100644
--- a/README.md
+++ b/README.md
@@ -2,7 +2,7 @@
 
 Code accompanying "Constrained Data Synthesis with Spectral Validation for Vector Surveillance Forecasting: Application to *Aedes aegypti* Early Warning" (submitted to INFORMS Journal on Data Science).
 
-This repository implements the paper's methodological contributions: SARIMAX gap reconstruction with literature-derived trend amplification, GMM-based breeding-intensity classification, a constrained random-walk synthetic data generator, Discrete Fourier Transform (DFT) spectral validation, KNN environmental matching and feature-panel assembly for synthetic traps, the pretrained environmental context autoencoder, and a two-stage LSTM+XGBoost hybrid forecasting model with out-of-fold residual generation and four-fold cross-validation. Raw C&oacute;rdoba ovitrap records are not included; see **Data availability** below.
+This repository implements the paper's methodological contributions: SARIMAX gap reconstruction with literature-derived trend amplification, GMM-based breeding-intensity classification, a constrained random-walk synthetic data generator, Discrete Fourier Transform (DFT) spectral validation, KNN environmental matching and feature-panel assembly for synthetic traps, the pretrained environmental context autoencoder, and a two-stage LSTM+XGBoost hybrid forecasting model with out-of-fold residual generation and four development-stage validation trials. Raw C&oacute;rdoba ovitrap records are not included; see **Data availability** below.
 
 ## Core contributions implemented here
 
@@ -15,7 +15,7 @@ This repository implements the paper's methodological contributions: SARIMAX gap
 - **`src/match_synthetic_traps_knn.py`** — Matches each synthetic trap coordinate to its nearest real 2009-2013 trap in normalized NDVI/NDBI/NDWI space.
 - **`src/build_feature_panel.py`** — Assembles the 23-raw-feature trap-week panel, its lag features (including the zero-filled Count_lag52 for the 31 test traps), and the (8, 23) sequence tensors.
 - **`src/train_environmental_autoencoder.py`** — Trains the frozen autoencoder that produces the 4-D environmental context embedding concatenated onto the feature panel.
-- **`src/forecast_hybrid_model.py`** — The two-stage Bidirectional LSTM + XGBoost residual-correction forecaster, with out-of-fold LSTM residual generation for the XGBoost training target and the four-fold cross-validation routine over the real breeding seasons.
+- **`src/forecast_hybrid_model.py`** — The two-stage Bidirectional LSTM + XGBoost residual-correction forecaster, with out-of-fold LSTM residual generation for the XGBoost training target and the four development-stage validation trials over the real breeding seasons.
 - **`src/tune_residual_models.py`** — Randomized hyperparameter search tuning XGBoost, Random Forest, and Gradient Boosting as residual correctors, using an identical feature set across all three and the same out-of-fold residual generation as the main forecaster.
 - **`src/evaluate_naive_baselines.py`** — Computes the persistence and climatology baselines on the held-out 2023-2024 test set, using the pooled-MSE convention described below.
 
@@ -32,7 +32,7 @@ This repository implements the paper's methodological contributions: SARIMAX gap
 | 7 | `src/match_synthetic_traps_knn.py` | `real_trap_coordinates_with_indices.csv` and `coordinates_with_indices.csv` from step 1 | `synthetic_trap_environmental_matches.csv` |
 | 8 | `src/build_feature_panel.py` | weekly egg counts (real and synthetic), NASA POWER climatic variables, the environmental indices from step 1, and the KNN matches from step 7 | `feature_panel.csv` (the 23-raw-feature trap-week panel) |
 | 9 | `src/train_environmental_autoencoder.py` | `feature_panel.csv` | `environmental_context_embedding.csv` (the 4-D embedding per trap-week) |
-| 10 | `src/forecast_hybrid_model.py` | `feature_panel.csv` and `environmental_context_embedding.csv`, assembled into (32, 8, 27) sequence tensors for the synthetic series, the real 2009-2013 seasons, and the held-out 2023-2024 test set | a trained LSTM, a trained XGBoost residual corrector, and four-fold cross-validation metrics |
+| 10 | `src/forecast_hybrid_model.py` | `feature_panel.csv` and `environmental_context_embedding.csv`, assembled into (32, 8, 27) sequence tensors for the synthetic series, the real 2009-2013 seasons, and the held-out 2023-2024 test set | a trained LSTM, a trained XGBoost residual corrector, and four development-stage validation trial metrics |
 | 11 | `src/tune_residual_models.py` | the same feature panel as step 10, plus LSTM predictions from an already-trained model | tuned Random Forest, Gradient Boosting, and XGBoost residual correctors and their comparison metrics |
 | 12 | `src/evaluate_naive_baselines.py` | `weekly_egg_counts_2009_2013.csv` (climatology) and `weekly_egg_counts_2023_2024.csv` (test trap-weeks) | `naive_baseline_metrics.csv` (persistence and climatology error metrics) |
 
diff --git a/REPRODUCE.md b/REPRODUCE.md
index 3eb5dbb..e7f34ed 100644
--- a/REPRODUCE.md
+++ b/REPRODUCE.md
@@ -38,7 +38,7 @@ Scripts in `src/` implementing the following stages of the pipeline. Each script
 
 Install dependencies with `pip install -r requirements.txt`.
 
-`tune_residual_models.py` is the residual-tuning procedure rather than a standalone entry point. It operates on an already-trained LSTM and an assembled feature panel, which it expects to be defined in the calling session, and generates its own training residuals out-of-fold via `compute_oof_lstm_predictions`, imported from `forecast_hybrid_model.py`. `forecast_hybrid_model.py` provides the model, training and cross-validation functions, which are called on the assembled feature panel.
+`tune_residual_models.py` is the residual-tuning procedure rather than a standalone entry point. It operates on an already-trained LSTM and an assembled feature panel, which it expects to be defined in the calling session, and generates its own training residuals out-of-fold via `compute_oof_lstm_predictions`, imported from `forecast_hybrid_model.py`. `forecast_hybrid_model.py` provides the model, training and development-stage validation functions, which are called on the assembled feature panel.
 
 Seeding is partial. `classify_breeding_intensity.py`, `reconstruct_sarimax_gap.py`, `match_synthetic_traps_knn.py`, and the XGBoost and inner-fold splits in `forecast_hybrid_model.py` and `tune_residual_models.py` are seeded. `synthesize_egg_count_series.py` and `train_environmental_autoencoder.py` are not seeded, so each run draws a different set of 2,000 series and a different autoencoder fit; the distributional constraints in Algorithm 1 of the manuscript, not a fixed seed, are what make the synthetic batch reproducible in the sense the paper claims. LSTM and autoencoder training are additionally subject to TensorFlow non-determinism.
 
diff --git a/src/forecast_hybrid_model.txt b/src/forecast_hybrid_model.txt
index 0621507..d786301 100644
--- a/src/forecast_hybrid_model.txt
+++ b/src/forecast_hybrid_model.txt
@@ -1,3 +1,3 @@
-Two-stage LSTM + XGBoost hybrid forecaster for Aedes aegypti egg counts, with 4-fold cross-validation
+Two-stage LSTM + XGBoost hybrid forecaster for Aedes aegypti egg counts, with four development-stage validation trials
 
-Bidirectional LSTM predicts weekly egg counts from an 8-week sequence of environmental, climatic, and autoregressive features; an XGBoost model then learns to correct the LSTM's residual error using the flattened input window, week number, and the LSTM's own prediction. Final forecast = LSTM output + XGBoost residual correction. Includes a 4-fold cross-validation routine over the real breeding seasons, each fold training on the synthetic series plus the other seasons and evaluating against a fixed held-out test set.
+Bidirectional LSTM predicts weekly egg counts from an 8-week sequence of environmental, climatic, and autoregressive features; an XGBoost model then learns to correct the LSTM's residual error using the flattened input window, week number, and the LSTM's own prediction. Final forecast = LSTM output + XGBoost residual correction. Includes four development-stage validation trials over the real breeding seasons, each trial training on the synthetic series plus the other seasons and evaluating against a fixed held-out test set.
diff --git a/src/tune_residual_models.txt b/src/tune_residual_models.txt
index 9c9822b..45bdfe8 100644
--- a/src/tune_residual_models.txt
+++ b/src/tune_residual_models.txt
@@ -1,3 +1,3 @@
 Randomized hyperparameter search for XGBoost, Random Forest, and Gradient Boosting residual correctors
 
-Tunes all three tree-based models via RandomizedSearchCV (20 sampled configurations each, 4-fold cross-validation on the training data) to correct the LSTM's residual error, using an identical 186-dimensional feature set across all three (flattened 8-week predictor window, week number, LSTM prediction), so results are directly comparable. Training residuals are generated out-of-fold via compute_oof_lstm_predictions (the same inner-fold procedure used in forecast_hybrid_model.py), so no model here is tuned against its own in-sample LSTM error. Selects the best configuration per model by cross-validated R² on the training data only, then reports its performance on the held-out test set. XGBoost is searched over the ranges in the paper's Appendix E (continuous sampling); Random Forest and Gradient Boosting over the discrete grids in Table 13.
+Tunes all three tree-based models via RandomizedSearchCV (20 sampled configurations each, 4-fold cross-validation on the training data) to correct the LSTM's residual error, using an identical 186-dimensional feature set across all three (flattened 8-week predictor window, week number, LSTM prediction), so results are directly comparable. Training residuals are generated out-of-fold via compute_oof_lstm_predictions (the same inner-fold procedure used in forecast_hybrid_model.py), so no model here is tuned against its own in-sample LSTM error. Selects the best configuration per model by cross-validated R² on the training data only, then reports its performance on the held-out test set. XGBoost is searched over the ranges in the paper's Appendix E (continuous sampling); Random Forest and Gradient Boosting over the discrete grids in Table 12.
diff --git a/tables/README.md b/tables/README.md
index da8fd42..b4dff8f 100644
--- a/tables/README.md
+++ b/tables/README.md
@@ -11,12 +11,12 @@ All 14 tables from the manuscript (main text and appendices) as static reference
 | `table5_performance_comparison.csv` | Table 5 | Held-out test performance of the LSTM and hybrid model, with and without synthetic augmentation |
 | `table6_fold_level_results.csv` | Table 6 | Per-trial results for the standalone LSTM and hybrid model |
 | `table7_hybrid_comparison_optimized.csv` | Table 7 | Hybrid model versus baseline comparison |
-| `table8_sensitivity_analysis.csv` | Table 8 | Augmentation design parameter sensitivity analysis |
-| `table9_notation.csv` | Table 9 (Appendix A) | Notation |
-| `table10_performance_metrics.csv` | Table 10 (Appendix B) | Forecast performance metrics and skill scores |
-| `table11_performance_comparison_full.csv` | Table 11 (Appendix B) | Full comparison including Random Forest, Gradient Boosting, persistence and climatology |
-| `table12_lstm_features.csv` | Table 12 (Appendix C) | The 23 LSTM predictor definitions |
-| `table13_rf_gb_config.csv` | Table 13 (Appendix E) | Randomized-search spaces for the RF and GB comparison models (20 configurations each) |
-| `table14_avg_correlation.csv` | Table 14 (Appendix F) | Average seasonal correlation between environmental indices and egg counts |
+| `table8_notation.csv` | Table 8 (Appendix A) | Notation |
+| `table9_performance_metrics.csv` | Table 9 (Appendix B) | Forecast performance metrics and skill scores |
+| `table10_performance_comparison_full.csv` | Table 10 (Appendix B) | Full comparison including Random Forest, Gradient Boosting, persistence and climatology |
+| `table11_lstm_features.csv` | Table 11 (Appendix C) | The 23 LSTM predictor definitions |
+| `table12_rf_gb_config.csv` | Table 12 (Appendix E) | Randomized-search spaces for the RF and GB comparison models (20 configurations each) |
+| `table13_avg_correlation.csv` | Table 13 (Appendix G) | Average seasonal correlation between environmental indices and egg counts |
+| `table14_sensitivity_analysis.csv` | Table 14 (Appendix J) | Augmentation design parameter sensitivity analysis |
 
 **Note on provenance:** these are static reference copies of the manuscript's tables. Where the scripts in `src/` compute the underlying numbers, they print or hold them in memory and do not write them in this format. See `REPRODUCE.md` for the script-to-section mapping.
diff --git a/tables/table11_performance_comparison_full.csv b/tables/table10_performance_comparison_full.csv
similarity index 100%
rename from tables/table11_performance_comparison_full.csv
rename to tables/table10_performance_comparison_full.csv
diff --git a/tables/table12_lstm_features.csv b/tables/table11_lstm_features.csv
similarity index 100%
rename from tables/table12_lstm_features.csv
rename to tables/table11_lstm_features.csv
diff --git a/tables/table13_rf_gb_config.csv b/tables/table12_rf_gb_config.csv
similarity index 100%
rename from tables/table13_rf_gb_config.csv
rename to tables/table12_rf_gb_config.csv
diff --git a/tables/table14_avg_correlation.csv b/tables/table13_avg_correlation.csv
similarity index 100%
rename from tables/table14_avg_correlation.csv
rename to tables/table13_avg_correlation.csv
diff --git a/tables/table8_sensitivity_analysis.csv b/tables/table14_sensitivity_analysis.csv
similarity index 52%
rename from tables/table8_sensitivity_analysis.csv
rename to tables/table14_sensitivity_analysis.csv
index e4465dc..23b8827 100644
--- a/tables/table8_sensitivity_analysis.csv
+++ b/tables/table14_sensitivity_analysis.csv
@@ -1,6 +1,6 @@
 Parameter,Value Used,Plausible Range,Validation Metric
-Confidence interval bound,90%,80%-95%,"80% yields cross-validation R2 in [0.6, 0.8] on the 2009-2013 development folds; 90% improves generalizability"
+Confidence interval bound,90%,80%-95%,"80% yields development-stage R2 in [0.6, 0.8] on the 2009-2013 development trials; 90% improves generalizability"
 GMM components,3,2-5,BIC model selection confirmed k=3
-Synthetic series count,"2,000","500-5,000","Cross-validation R2 ~0.50-0.67 at 1,000 series on the development folds; rises to optimum at 2,000; further gains diminish and overfitting risk increases beyond 2,000"
+Synthetic series count,"2,000","500-5,000","Development-stage R2 ~0.50-0.67 at 1,000 series on the development trials; rises to optimum at 2,000; further gains diminish and overfitting risk increases beyond 2,000"
 LSTM lookback window,8 weeks,4-12 weeks,R2 range of +/-0.007 across 4-12 weeks; 8 weeks Optuna-confirmed optimal
 SARIMAX model order,Fitted to 2009-2013,+/-1 AR/MA order,DFT Error_k at dominant frequency stays low across +/-1 order; downstream test R2 varies by no more than +/-0.006 across the same range
diff --git a/tables/table9_notation.csv b/tables/table8_notation.csv
similarity index 100%
rename from tables/table9_notation.csv
rename to tables/table8_notation.csv
diff --git a/tables/table10_performance_metrics.csv b/tables/table9_performance_metrics.csv
similarity index 100%
rename from tables/table10_performance_metrics.csv
rename to tables/table9_performance_metrics.csv
